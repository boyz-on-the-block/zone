import { ITokenParser } from "../ParserFactory";
import * as strtok3 from "strtok3";
import { INativeAudioMetadata, IOptions } from "../index";
/**
 * Vorbis 1 Parser.
 * Used by OggParser
 */
export declare class VorbisParser implements ITokenParser {
    private format;
    private tags;
    private tokenizer;
    private options;
    parse(tokenizer: strtok3.ITokenizer, options: IOptions): Promise<INativeAudioMetadata>;
    /**
     * Vorbis 1 parser
     * @param pageLength
     * @returns {Promise<void>}
     */
    private parseHeaderPacket();
    private parsePacket(packetType);
    private parseVorbisInfo();
    /**
     * Ref: https://xiph.org/vorbis/doc/Vorbis_I_spec.html#x1-840005.2
     * @returns {Promise<number>}
     */
    private parseUserCommentList();
    private parseUserComment(userCommentListLength);
}
