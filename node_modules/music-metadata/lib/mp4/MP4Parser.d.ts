import { ITokenParser } from "../ParserFactory";
import { INativeAudioMetadata, IOptions } from "../";
import { ITokenizer } from "strtok3";
export declare class MP4Parser implements ITokenParser {
    private static Types;
    private static read_BE_Signed_Integer(value);
    private static read_BE_Unsigned_Integer(value);
    private tokenizer;
    private options;
    private metaAtomsTotalLength;
    private format;
    private tags;
    private warnings;
    parse(tokenizer: ITokenizer, options: IOptions): Promise<INativeAudioMetadata>;
    parseAtom(): Promise<boolean>;
    private parseAtomData(header);
    private ignoreAtomData(len);
    private parseAtom_ftyp(len);
    /**
     * Parse movie header (mvhd) atom
     * @param len
     */
    private parseAtom_mvhd(len);
    /**
     * Parse media header (mdhd) atom
     * @param len
     */
    private parseAtom_mdhd(len);
    private parse_mxhd(mxhd);
    /**
     * Parse media header (ilst) atom
     * @param len
     * Ref: https://developer.apple.com/library/content/documentation/QuickTime/QTFF/Metadata/Metadata.html#//apple_ref/doc/uid/TP40000939-CH1-SW8
     */
    /**
     * Parse Meta-item-list-atom (item of 'ilst' atom)
     * @param len
     * Ref: https://developer.apple.com/library/content/documentation/QuickTime/QTFF/Metadata/Metadata.html#//apple_ref/doc/uid/TP40000939-CH1-SW8
     */
    private parseMetadataItem(len);
    private parseMetadataItemData(tagKey, remLen);
    private parseValueAtom(tagKey, header);
}
